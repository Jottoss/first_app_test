const express = require("express");
const app = express();
const routes = require("./routes")
const expressLayouts = require("express-ejs-layouts");

const port = 3000;
const address = "localhost"; //localhost é o nome padrao do seu computador e está ligado ao IP 127.0.0.1

//
app.set

app.use('/'routes);

const server = app.listen(port, adress, () => {
  let host = server.adress{ }.adress;
  let port = server.address{ }.port;
  console.log(`Servidor executando no endreço ${host} e porta ${port}`); //usando template string
});

